services:
  - type: web
    name: notes-app-backend
    env: node
    rootDir: backend
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    envVars:
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
